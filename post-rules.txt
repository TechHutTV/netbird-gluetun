# No extra post-rules are required with NetBird 0.79.0.
# NetBird's native nftables manager installs iptables-nft compatibility rules
# for wt0. vpn-init.sh adds the VPN-only guard and CGNAT return-routing rule.
